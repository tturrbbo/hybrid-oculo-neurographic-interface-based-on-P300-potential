"""
Онлайн-анализ P300: разбор маркеров LSL, выравнивание эпох, метрики победителя, окно Qt.

Основные модули:
- ``constants`` — длительность эпохи, лимиты буферов;
- ``marker_parsing`` — строки маркеров → класс стимула;
- ``signal_processing`` — baseline и интеграл;
- ``winner_selection`` — режимы выбора класса;
- ``epoch_geometry`` — сетка времени и индекс начала эпохи;
- ``erp_compute`` — усреднение и расчёт для графиков;
- ``lsl_streams`` — поиск потоков и inlet;
- ``qt_window`` — интерфейс ``P300AnalyzerWindow``.
"""
